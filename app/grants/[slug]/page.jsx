import GrantDetails from "@/components/grants/GrantDetails";

export const metadata = {
  title: "Grant Details | GrantPilot AI",
  description: "Review approved grant details, eligibility, and funding signals.",
};

export default function GrantDetailsPage() {
  return <GrantDetails />;
}
